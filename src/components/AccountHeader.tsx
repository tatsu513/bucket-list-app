import styles from '../assets/styles/modules/AccountHeader.module.scss';

const AccountHeader = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.title}>100 BUCKET LIST</h1>
    </header>
  );
};

export default AccountHeader;
